{
  "name": "alshifa-diagnostic-centre",
  "version": "1.0.0",
  "private": true,
  "description": "Al-Shifa Diagnostic Centre - offline desktop hospital management system",
  "workspaces": [
    "client",
    "server",
    "electron"
  ],
  "scripts": {
    "dev": "concurrently -k -n SERVER,CLIENT,ELECTRON -c green,cyan,magenta \"npm run dev --workspace=server\" \"npm run dev --workspace=client\" \"wait-on tcp:5173 tcp:4000 && npm run dev --workspace=electron\"",
    "prisma:generate": "prisma generate --schema=prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema=prisma/schema.prisma",
    "prisma:studio": "prisma studio --schema=prisma/schema.prisma",
    "prisma:seed": "node prisma/seed.js",
    "build:client": "npm run build --workspace=client",
    "postinstall": "npm run prisma:generate"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.2",
    "prisma": "^6.5.0"
  }
}
